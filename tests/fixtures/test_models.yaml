models:
  test-model:
    provider: openai
    model_id: gpt-3.5-turbo
    capabilities:
      - code_generation
      - prompt_generation
    max_tokens: 1000
    temperature: 0.7
    supports_system_prompt: true

provider_configs:
  openai:
    api_key_env: OPENAI_API_KEY_TEST
