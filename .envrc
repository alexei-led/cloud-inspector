#!/usr/bin/env fish

# Use direnv's built-in PATH_add function to prepend .venv/bin
PATH_add ".venv/bin"

# Load environment variables from .env file
dotenv_if_exists .env

# Setup Python virtual environment
layout python


